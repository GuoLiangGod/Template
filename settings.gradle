pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Template"
include ':app'
include ':frame'
include ':customView'

project(':frame').projectDir = new File('framework/frame')
project(':customView').projectDir = new File('framework/customView')