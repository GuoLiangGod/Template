pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Template"
include ':app'
include ':frameKT'
include ':customView'

project(':frameKT').projectDir = new File('framework/frameKT')
project(':customView').projectDir = new File('framework/customView')